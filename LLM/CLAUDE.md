# CLAUDE.md — ui-compass-web-designs

You (Claude or any other LLM) have been handed a static 11ty site exported from **Page Builder**. This doc is your entry point. Read it fully before editing anything.

## What this project is

- Static site built with [Eleventy](https://www.11ty.dev/) (v2 config in `.eleventy.js`).
- Hosted on [Cloudflare Pages](https://pages.cloudflare.com/), with optional serverless endpoints in `functions/` and an optional D1 database (`database/schema.sql`).
- The HTML you see in `src/` was generated by Page Builder from CodeStitch section sources and a template scrape. **The generator is NOT in this repo.** If you re-run anything, it is the 11ty build only (`npm run build`), not Page Builder.

## Active feature packs

- **Decap CMS (content editing at /admin/)** (`cms`)
- **Contact Form (D1-backed)** (`forms`)
- **PWA / Service Worker** (`pwa`)
- **Sitemap** (`sitemap`)
- **Multi-Language (11ty-plugin-i18n)** (`i18n`)
- **Push Notifications** (`pushNotifications`)
- **Blog** (`blog`)
- **Cron Scheduler** (`cron`)
- **E-Commerce (shop admin)** (`shop`)
- **Client Portal** (`portal`)
- **Messaging** (`messaging`)
- **E-Sign / Contracts** (`esign`)
- **Invoicing** (`invoicing`)
- **Media Delivery** (`media`)
- **pageViewTransitions** (`pageViewTransitions`)
- **hapticFeedback** (`hapticFeedback`)

### Cloudflare Functions endpoints shipped

- `functions/api/auth.js` — Decap CMS (content editing at /admin/)
- `functions/api/form.js` — Contact Form (D1-backed)
- `functions/api/push/**` — Push Notifications
- `functions/api/blog/**` — Blog
- `functions/api/shop/**` — E-Commerce (shop admin)
- `functions/api/portal/**` — Client Portal
- `functions/api/messaging/**` — Messaging
- `functions/api/esign/**` — E-Sign / Contracts
- `functions/api/invoicing/**` — Invoicing
- `functions/api/media/**` — Media Delivery

### Pack-specific CSS namespaces in play

- `.blog-*` — Blog
- `.shop-*` — E-Commerce (shop admin)
- `.p-*` — Client Portal
- `.msg-*` (standalone) / `.m-*` (CRM tab) — Messaging
- `.esign-*` — E-Sign / Contracts
- `.inv-*` — Invoicing
- `.media-*` — Media Delivery

Every section also carries a `[data-pb-section="<id>"]` scoping attribute. All section-scoped CSS in `src/css/local.css` is wrapped by that selector — do **not** flatten or rewrap it.

## Pages in this export

- **Home** → slug: `index`, file: `src/index.html`, permalink: `/` (9 section(s))
- **Abingdon Small Business Web Designer** → slug: `abingdon-small-business-web-designer`, file: `src/pages/abingdon-small-business-web-designer.html`, permalink: `/abingdon-small-business-web-designer/` (3 section(s))
- **Blog Content Is Still King Understanding Website Content Importance For Web Page Seo** → slug: `blog-content-is-still-king-understanding-website-content-importance-for-web-page-seo`, file: `src/pages/blog-content-is-still-king-understanding-website-content-importance-for-web-page-seo.html`, permalink: `/blog-content-is-still-king-understanding-website-content-importance-for-web-page-seo/` (2 section(s))
- **Blog** → route owned by Blog pack (`/blog/`), no `src/pages/blog-2.html` emitted (1 section(s))
- **Contact** → slug: `contact`, file: `src/pages/contact.html`, permalink: `/contact/` (3 section(s))
- **Discovery** → slug: `discovery`, file: `src/pages/discovery.html`, permalink: `/discovery/` (1 section(s))
- **Ecommerce** → slug: `ecommerce`, file: `src/pages/ecommerce.html`, permalink: `/ecommerce/` (2 section(s))
- **Free Audit** → slug: `free-audit`, file: `src/pages/free-audit.html`, permalink: `/free-audit/` (2 section(s))
- **Hosting And Domains** → slug: `hosting-and-domains`, file: `src/pages/hosting-and-domains.html`, permalink: `/hosting-and-domains/` (2 section(s))
- **Locations** → slug: `locations`, file: `src/pages/locations.html`, permalink: `/locations/` (3 section(s))
- **Our Story** → slug: `our-story`, file: `src/pages/our-story.html`, permalink: `/our-story/` (6 section(s))
- **Portfolio** → slug: `portfolio`, file: `src/pages/portfolio.html`, permalink: `/portfolio/` (2 section(s))
- **Pricing** → slug: `pricing`, file: `src/pages/pricing.html`, permalink: `/pricing/` (4 section(s))
- **Search Engine Optimisation** → slug: `search-engine-optimisation`, file: `src/pages/search-engine-optimisation.html`, permalink: `/search-engine-optimisation/` (2 section(s))
- **Unlimited Edits And Support** → slug: `unlimited-edits-and-support`, file: `src/pages/unlimited-edits-and-support.html`, permalink: `/unlimited-edits-and-support/` (2 section(s))
- **Web Design** → slug: `web-design`, file: `src/pages/web-design.html`, permalink: `/web-design/` (2 section(s))
- **Web Development** → slug: `web-development`, file: `src/pages/web-development.html`, permalink: `/web-development/` (2 section(s))
- **Why Us** → slug: `why-us`, file: `src/pages/why-us.html`, permalink: `/why-us/` (6 section(s))


## Starting template reference

Starting template: **testvalleydigital.com**

The original scrape lives at `_reference/original-template/`. Treat it as **read-only**; it is not part of the 11ty build. Use it to diff against the current `src/_includes/header.html`, `src/_includes/footer.html`, and per-page content in `src/pages/*.html` when you are asked to restore or replicate visuals.


## Sections placed in this export

The raw CodeStitch HTML dump for every placed section lives under `_reference/section-sources/`. These are the **pre-customization** sources — use them to see what markup the section originally shipped with, before Page Builder applied edits, asset replacement, and `[data-pb-section]` scoping.

- **blog**
  - `template-testvalleydigital.com-blog-1580` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **cta**
  - `template-bluffcitywebdesigns.com-cta-697` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-cta-1072` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **faq**
  - `template-bluffcitywebdesigns.com-faq-1261` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **footer**
  - `stitch-1392-0` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-cs-footer-309` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **forms-contact**
  - `template-testvalleydigital.com-contact-1600` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-cta-1612` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **hero**
  - `template-testvalleydigital.com-hero-1950` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **interior-pages**
  - `template-testvalleydigital.com-banner-1106` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-content-page-1399` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **meet-the-team**
  - `template-testvalleydigital.com-meet-494` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **navigation**
  - `template-testvalleydigital.com-cs-navigation` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **portfolio**
  - `template-oakharborwebdesigns.com-portfolio` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **pricing**
  - `template-oakharborwebdesigns.com-pricing-1690` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-pricing-2063` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-pricing-2184` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **reviews**
  - `template-bluffcitywebdesigns.com-reviews-211` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-reviews-1335` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-reviews-989` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **services**
  - `template-testvalleydigital.com-services-1628` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **side-by-side**
  - `template-testvalleydigital.com-sbs-2295` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-sbsr-2232` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-sbs-1640` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-sbsr-1363` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **stats**
  - `template-testvalleydigital.com-stats-1643` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **steps**
  - `template-testvalleydigital.com-steps-585` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **unknown**
  - `template-testvalleydigital.com-outer-wrapper` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
  - `template-testvalleydigital.com-service` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)
- **why-choose-us**
  - `template-testvalleydigital.com-why-choose-1371` → (no source file mirrored — run `npm run build:reference-bundles` and re-export)


## Directory map

| Path | What it is | Safe to edit? |
|---|---|---|
| `src/_layouts/base.html` | Generated site shell. CSP, OG tags, font preloads. | Yes, carefully. |
| `src/_includes/header.html` / `footer.html` | Generated shared chrome from Page Builder export. | **No (generated baseline)**. Edit only if explicitly requested. |
| `src/pages/*.html` | Per-page section HTML. | Yes — this is where most edits happen. |
| `src/css/root.css` | Theme variables + global base styles. | Yes. |
| `src/css/critical.css` | Above-the-fold subset of local.css. | **No (generated artifact)** unless explicitly asked. |
| `src/css/local.css` | All per-section CSS, scoped by `[data-pb-section]`. | Yes, but preserve the scoping selectors. |
| `src/assets/js/sections.js` | Per-section JS bundled at build time. | Yes. |
| `src/_data/client.json` | Business info, address, hours — surfaced across sections. | Yes. |
| `functions/api/**` | Cloudflare Pages Functions (Workers runtime). | Yes — but test against local `wrangler pages dev`. |
| `database/schema.sql` | D1 schema baseline generated by export. | **No (generated baseline)**. Use migrations for deployed DB changes. |
| `public/` | Build output. | **No.** Delete and rebuild with `npm run build`. |
| `_reference/` | Read-only reference material — original template + raw section sources. Not part of the build. | **No.** Safe to delete if you don't want an LLM in the loop. |
| `LLM/` | This doc and its companion rules. | Edit freely to refine guidance for the next session. |

## Image source audit

If this export downloaded third-party images for offline hosting, `src/assets/images/_sources.json` lists each local file and its original URL. This is an audit trail only; it is **not** a license grant. Review image rights before publishing, and replace any image the client does not own or have permission to use.

## Edit-safe zones vs. don't-touch

**Safe to edit directly:**
- Any `src/pages/*.html`, `src/css/root.css`, `src/css/local.css`, `src/_data/*.json`.
- Any `functions/api/**/*.js` (but keep D1 bindings as `env.DB`).
- `package.json`, `.eleventy.js`, `wrangler.toml` (modify with care).

**Don't touch without a very good reason:**
- `src/_includes/header.html` and `src/_includes/footer.html` — generated shared section output from Builder export.
- `database/schema.sql` — generated baseline schema; for production changes create migrations instead.
- The `[data-pb-section="..."]` attributes — other CSS and the per-section JS scoping depends on them.
- `public/` — rebuild output; any hand edits will vanish on next `npm run build`.
- CSP headers in `src/_headers` — tighten only; loosening needs review.

## When you need more context

- Read `LLM/EXECUTION-RULES.md` for discipline rules (never invent selectors, always read before writing, etc.).
- Read the raw CodeStitch source for a section in `_reference/section-sources/<category>/` before restructuring it.
- Read the matching page in `_reference/original-template/` before a visual restoration task.

## Parked / disabled code

Some components have been built, deemed not currently needed, and **left in the repo in a disabled (inert) state** so they can be restored quickly without rebuilding from scratch. **Never delete these files.** If you stumble on what looks like dead code, check this list first.

### Inline pricing widget (`#price-widget-1701`) — disabled 2026-05-09

A vanilla-JS interactive pricing calculator that was briefly the contents of `/pricing/`. Reverted to the static `#pricing-1690` cards (same as the homepage). The widget files remain on disk in inert form:

| File | Disabled state |
|---|---|
| [src/pages/pricing.html](src/pages/pricing.html) | Widget block + its `<link>` and `<script>` tags wrapped in `<!-- ... -->`. The static `#pricing-1690` section above it is the live one. |
| [src/css/pricing-widget.css](src/css/pricing-widget.css) | Entire stylesheet wrapped in `@media (width: 0px) { ... }` — never matches a real viewport. Marker comments: `WIDGET-DISABLED-WRAPPER-START` / `WIDGET-DISABLED-WRAPPER-END`. |
| [src/assets/js/pricing-widget.js](src/assets/js/pricing-widget.js) | IIFE is defined but NOT invoked — the trailing `()` was removed from the very last line. Marker comment: `WIDGET-DISABLED — to restore, change \`)\` to \`)()\`` at the end of file. |

**To restore the widget and remove the static cards:**
1. In [src/pages/pricing.html](src/pages/pricing.html): delete the entire `<section id="pricing-1690">…</section>` block, then delete the surrounding `<!-- … -->` comment around the widget below it (keeping the widget HTML itself).
2. In [src/css/pricing-widget.css](src/css/pricing-widget.css): delete the line `@media (width: 0px) { /* WIDGET-DISABLED-WRAPPER-START */` and the matching closing `} /* WIDGET-DISABLED-WRAPPER-END … */` at the bottom.
3. In [src/assets/js/pricing-widget.js](src/assets/js/pricing-widget.js): change the final line's `});` back to `})();`.
4. `npm run build` and verify `/pricing/` shows the interactive widget instead of the three static cards.

The widget is **only intended for `/pricing/`**. The static `#pricing-1690` section that lives on the homepage (`src/index.html`), the Spanish pricing page (`src/pages/es/pricing.html`), and the Spanish homepage (`src/pages/es/index.html`) was never replaced and should remain untouched.

## Build and deploy locally

```bash
npm install
npm start        # dev server at http://localhost:8080
npm run build    # build to public/
```

Setup and deploy steps are in `SETUP.html` (open it in a browser) and `README.md`.
