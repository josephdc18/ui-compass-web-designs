A hand-coded site loads up to 10× faster than a page-builder site — and the gap isn't configurable.

Page builders assemble pages on demand: database hit, theme load, plugin chain, JavaScript on the server, then the HTML ships. Hand-coded sites are already there. The HTML exists. Server hands it over. Time to first byte: 60ms vs 800ms+.

The cost shows up in conversion drop, mobile performance, and platform lock-in. The choice locks in the day you pick.

Full breakdown — the wire-level math, the comparison numbers, and what it means if you're picking a platform today:

https://uicompass.com/blog/the-10x-load-time-gap/

Curious how yours measures up? Drop your URL.
