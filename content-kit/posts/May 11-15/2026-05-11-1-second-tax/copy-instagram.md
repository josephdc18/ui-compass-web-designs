7%.

That's the conversion drop for every second your site takes to load.

Multiply that by your monthly leads — that's the bill.

Where the second goes:
→ One unoptimized hero image (500ms)
→ Page-builder framework you never see (200–400KB)
→ Shared hosting (800ms TTFB)
→ Render-blocking fonts (500ms)

All cheap to fix on a hand-coded site. A rebuild on a page builder.

Full breakdown in the new post — link in bio (or DM "tax").

#UICompass #DallasWebDesign #SmallBusinessWebsite #Performance #PageSpeed
